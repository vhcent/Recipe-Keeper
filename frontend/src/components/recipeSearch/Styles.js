import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    text1: { 
        fontSize: 20,
        color: "blue",
        margin: 10,
        marginTop: 20,
        marginBottom: 20,
        textAlign: "center",
    }
})

export default styles;